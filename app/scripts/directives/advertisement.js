'use strict';

/**
 * @ngdoc directive
 * @name waktusolatmyApp.directive:advertisement
 * @description
 * # advertisement
 */
angular.module('waktusolatmyApp')
  .component('advertisement', {
    templateUrl: 'views/advertisement.html',
    controller: ['$log',
      function advertisement($log) {
        var $ctrl = this;


      }]
  });
